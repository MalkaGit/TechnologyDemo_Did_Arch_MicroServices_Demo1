goal:
	API gateway accepts a command and publish it to the message bus 
    eg:  await this._BusClient.PublishAsync(command);

1.  Create TestMessagingCommand, TestMessagingRejectedEvent,TestMessagingRejectedEvent

1.	X.GateWay.ApiApp

    1.1 Data memeber: service bus

    1.2 Constructor:    accepts service bus  (using IOC  to make it i rabbit mq)

	1.3 POST method to accept command and publish it 
     
        /// <summary>
        /// Tst the publish commnad
        /// </summary>
        /// <param name="command"></param>
        /// <returns></returns>
        [HttpPost("")]
        public async Task<IActionResult> Post([FromBody] TestMessagingCommand command)
        { 
            await this._BusClient.PublishAsync(command);

            return Accepted();
        }



    1.2 Test 
        http://localhost:54070/testPublish
